import React from 'react';

const Drafts = (user) => {
    console.log(user);

        return(
            <div style={{height: '10em', padding: '2em'}}>
                Borradores
            </div>
        );

}

export default Drafts;